# Lightweight requirements for Render deployment
# Excludes PyTorch and heavy ML packages (Modal handles AI inference)
# This significantly reduces build time and avoids connection timeouts

# FastAPI and web server
fastapi==0.117.1
uvicorn==0.36.0
gunicorn==23.0.0
starlette==0.48.0
python-multipart==0.0.12
sse-starlette==2.1.3

# Pydantic and validation
pydantic==2.11.9
pydantic_core==2.33.2
annotated-types==0.7.0
typing-extensions==4.15.0
typing-inspection==0.4.1

# Database (PostgreSQL + pgvector)
SQLAlchemy==2.0.35
psycopg2-binary>=2.9.9
pgvector==0.2.5
alembic==1.13.3

# Redis
redis==5.0.8

# Authentication
PyJWT==2.9.0

# Monitoring and observability
sentry-sdk==2.18.0
prometheus-fastapi-instrumentator==7.0.0
opentelemetry-instrumentation-fastapi==0.49b0
asgi-correlation-id==4.3.1

# Rate limiting
slowapi==0.1.9

# Scheduling
APScheduler==3.10.4

# Utilities
python-dotenv==1.0.1
requests==2.32.5
pycountry==24.6.1

# Core dependencies (minimal set)
anyio==4.10.0
certifi==2025.8.3
charset-normalizer==3.4.3
click==8.3.0
idna==3.10
packaging==25.0
setuptools==80.9.0
sniffio==1.3.1
urllib3==2.5.0

# Testing (optional, but keep for now)
pytest==8.3.3

