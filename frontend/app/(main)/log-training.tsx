import { View } from 'react-native';

// Placeholder — the "+" tab button opens a sheet instead of navigating here.
export default function LogTrainingScreen() {
    return <View style={{ flex: 1 }} />;
}